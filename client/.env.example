# Leave empty: the client calls the API on the same origin (/api).
# In development Vite proxies /api to http://localhost:5000.
VITE_API_BASE_URL=

# Only used by the legacy standalone server in client/server/index.js
MONGODB_URI=mongodb://127.0.0.1:27017
MONGODB_DB=deadly_assault
PORT=5000
